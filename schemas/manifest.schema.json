{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/try-hola/apps/schemas/manifest.schema.json",
  "title": "Hola app manifest.json",
  "description": "Shape of src/<name>/src/manifest.json, the per-app metadata the Hola server reads at deploy time. See README.md for full field docs. additionalProperties:false is used at the top level and for defaultEnv entries because the Hola server's manifest coercion silently drops unknown/malformed fields instead of erroring -- a typo'd field name is otherwise a silent no-op, not a CI failure.",
  "type": "object",
  "additionalProperties": false,
  "required": ["name", "version", "title", "description", "ingress"],
  "properties": {
    "name": {
      "type": "string",
      "minLength": 1,
      "description": "App id; should match the package name."
    },
    "version": {
      "type": "string",
      "minLength": 1,
      "description": "Hola bundle version (describes impact on the Hola user, not upstream's version)."
    },
    "title": { "type": "string", "minLength": 1 },
    "description": { "type": "string", "minLength": 1 },
    "icon": {
      "type": "string",
      "description": "Emoji or an icon URL."
    },
    "category": { "type": "string" },
    "tags": {
      "type": "array",
      "items": { "type": "string" }
    },
    "ingress": {
      "type": "object",
      "additionalProperties": false,
      "required": ["service", "port"],
      "properties": {
        "service": {
          "type": "string",
          "minLength": 1,
          "description": "Compose service Traefik routes to and that receives injected SSO/auth env. Must name a real service in compose.yaml."
        },
        "port": {
          "type": "integer",
          "minimum": 1,
          "maximum": 65535,
          "description": "Container port that service listens on."
        }
      }
    },
    "defaultEnv": {
      "type": "array",
      "items": { "$ref": "#/$defs/appEnvVar" }
    },
    "defaults": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "ports": {
          "type": "array",
          "items": {
            "type": "object",
            "additionalProperties": true,
            "required": ["container"],
            "properties": {
              "host": { "type": "integer" },
              "container": { "type": "integer" },
              "protocol": { "type": "string", "enum": ["tcp", "udp"] }
            }
          }
        },
        "volumes": {
          "type": "array",
          "items": {
            "type": "object",
            "additionalProperties": true,
            "required": ["containerPath"],
            "properties": {
              "hostPath": { "type": "string" },
              "containerPath": { "type": "string" },
              "readOnly": { "type": "boolean" }
            }
          }
        }
      }
    },
    "auth": {
      "type": "object",
      "description": "SSO integration. Permissive on nested shapes -- this block is not part of the typed-defaultEnv contract and the server's coercion is deliberately forward-compatible about extra fields.",
      "additionalProperties": true,
      "required": ["mode"],
      "properties": {
        "mode": {
          "type": "string",
          "enum": ["none", "native-oidc", "forward-auth", "native-ldap"]
        },
        "fallback": {
          "type": "string",
          "enum": ["forward-auth"],
          "description": "Gates a native-oidc/none app behind proxy login too; can coexist with any mode."
        },
        "oidc": {
          "type": "object",
          "additionalProperties": true,
          "properties": {
            "redirectPath": { "type": "string" },
            "scopes": { "type": "array", "items": { "type": "string" } },
            "env": {
              "type": "object",
              "additionalProperties": { "type": "string" },
              "properties": {
                "issuer": { "type": "string" },
                "clientId": { "type": "string" },
                "clientSecret": { "type": "string" },
                "redirectUri": { "type": "string" },
                "authUrl": { "type": "string" },
                "tokenUrl": { "type": "string" },
                "userinfoUrl": { "type": "string" }
              }
            },
            "staticEnv": {
              "type": "object",
              "additionalProperties": { "type": "string" }
            },
            "setup": {
              "type": "object",
              "additionalProperties": true,
              "required": ["command"],
              "properties": {
                "service": { "type": "string" },
                "user": { "type": "string" },
                "check": { "type": "array", "items": { "type": "string" } },
                "checkMatch": { "type": "string" },
                "command": { "type": "array", "items": { "type": "string" } }
              }
            },
            "extraRedirectUris": {
              "type": "array",
              "items": { "type": "string" }
            },
            "credentialsFile": {
              "type": "object",
              "additionalProperties": true,
              "required": ["path"],
              "properties": {
                "path": { "type": "string" }
              }
            },
            "roleClaim": {
              "type": "object",
              "additionalProperties": true,
              "required": ["claim"],
              "properties": {
                "claim": { "type": "string" },
                "adminGroup": { "type": "string" },
                "adminValue": { "type": "string" },
                "memberValue": { "type": "string" },
                "scope": { "type": "string" }
              }
            }
          }
        },
        "ldap": {
          "type": "object",
          "additionalProperties": true,
          "required": ["env"],
          "properties": {
            "env": {
              "type": "object",
              "additionalProperties": { "type": "string" },
              "required": ["host", "port", "bindDn", "bindPassword", "baseDn"],
              "properties": {
                "host": { "type": "string" },
                "port": { "type": "string" },
                "bindDn": { "type": "string" },
                "bindPassword": { "type": "string" },
                "baseDn": { "type": "string" }
              }
            }
          }
        },
        "forwardAuth": {
          "type": "object",
          "additionalProperties": true,
          "properties": {
            "allowedGroups": {
              "type": "array",
              "items": { "type": "string" }
            }
          }
        }
      }
    },
    "consumes": {
      "description": "Cross-app capability(ies) this app consumes, e.g. 'app-registry', 'apps-data'. A bare string or an array of strings.",
      "anyOf": [
        { "type": "string" },
        { "type": "array", "items": { "type": "string" } }
      ]
    },
    "provides": {
      "description": "Capability contract(s) this app PERFORMS for other apps, as `id@version` refs (e.g. 'backup@1'). Reserved for the small number of apps that act on other apps' data; grants privilege, so it is reviewed at publish time and consented to by the operator at install. A bare string or an array of strings.",
      "anyOf": [
        { "type": "string", "pattern": "^[a-z0-9][a-z0-9-]*@[0-9]+$" },
        { "type": "array", "items": { "type": "string", "pattern": "^[a-z0-9][a-z0-9-]*@[0-9]+$" } }
      ]
    },
    "accepts": {
      "description": "Capability contract(s) this app is a SUBJECT of, as `id@version` refs (e.g. 'backup@1'). Declare it whenever the app is covered — including when it needs no hooks at all — because silence is reported as 'not covered', not as fine. A bare string or an array of strings.",
      "anyOf": [
        { "type": "string", "pattern": "^[a-z0-9][a-z0-9-]*@[0-9]+$" },
        { "type": "array", "items": { "type": "string", "pattern": "^[a-z0-9][a-z0-9-]*@[0-9]+$" } }
      ]
    },
    "upgrade": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "breaking": { "type": "boolean" },
        "minFromVersion": { "type": "string" },
        "waypoints": { "type": "array", "items": { "type": "string" } },
        "upgradeNotesUrl": { "type": "string" },
        "preUpgradeBackup": {
          "type": "string",
          "enum": ["required", "recommended", "none"]
        }
      }
    },
    "backup": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "preHook": { "$ref": "#/$defs/backupHook" },
        "postHook": { "$ref": "#/$defs/backupHook" }
      }
    },
    "push": {
      "type": "array",
      "minItems": 1,
      "description": "Directories this app accepts bulk data into, pushed with `hola app data push`. For data too big or too structured for the app's own upload (an ebook library, a media tree, a document archive). Omit unless the app has such a directory.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["id", "label", "path"],
        "properties": {
          "id": {
            "type": "string",
            "pattern": "^[a-z0-9][a-z0-9-]*$",
            "description": "Stable identifier the CLI takes as an argument (e.g. `library`). Unique within this app."
          },
          "label": {
            "type": "string",
            "minLength": 1,
            "description": "Human-friendly name shown by `hola app data push <app> --list`."
          },
          "description": {
            "type": "string",
            "minLength": 1,
            "description": "What the operator should point at this target."
          },
          "path": {
            "type": "string",
            "minLength": 1,
            "pattern": "^[^/\\s][^\\s]*$",
            "description": "Directory RELATIVE to the app's data root (e.g. `books` for a /books bind under ${HOLA_APP_DATA}). Never absolute, never a container path, and never containing `..` — the server resolves it against the data root and rejects anything that escapes."
          },
          "mode": {
            "type": "string",
            "enum": ["mirror", "additive"],
            "default": "additive",
            "description": "mirror: rsync --delete, so the server copy matches the local one exactly (files only on the server are DELETED). additive (default): copy in without deleting. Declare mirror only when the directory genuinely is a replica of the operator's copy."
          },
          "quiesce": {
            "type": "string",
            "enum": ["stop", "none"],
            "default": "none",
            "description": "stop: stop the app for the duration of the push and start it after — for apps that hold open handles on this data (e.g. a SQLite database read at startup). none (default): leave it running."
          },
          "postHook": {
            "$ref": "#/$defs/backupHook",
            "description": "Run in one of the app's own services after the push — a reindex/reconnect, for apps that would otherwise need a `quiesce: stop` bounce. Same exec-form shape as a backup hook, and subject to the same ~60s budget."
          }
        }
      }
    },
    "security": {
      "type": "object",
      "additionalProperties": false,
      "description": "Elevated container permissions this app requests. Each relaxes the platform's default container hardening and is surfaced in the install wizard for explicit operator consent. Omit for the fully-hardened default.",
      "properties": {
        "elevated": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": ["type", "reason"],
            "properties": {
              "type": {
                "type": "string",
                "enum": ["allow-privilege-escalation"],
                "description": "allow-privilege-escalation: drop no-new-privileges on the ingress service so setuid escalation (sudo) works. Needed by browser-desktop apps."
              },
              "reason": {
                "type": "string",
                "minLength": 1,
                "description": "Why the app needs this grant. Shown next to the consent checkbox so the operator can make an informed decision."
              }
            }
          }
        }
      }
    }
  },
  "$defs": {
    "backupHook": {
      "type": "object",
      "additionalProperties": false,
      "required": ["service", "command"],
      "properties": {
        "service": {
          "type": "string",
          "minLength": 1,
          "description": "Compose service the hook runs in (via docker compose exec)."
        },
        "command": {
          "type": "array",
          "minItems": 1,
          "items": { "type": "string" },
          "description": "Exec-form argv, not a shell string."
        }
      }
    },
    "paramType": {
      "type": "string",
      "enum": ["string", "integer", "port", "boolean", "enum", "url", "email", "timezone"]
    },
    "paramGenerate": {
      "type": "object",
      "additionalProperties": false,
      "required": ["kind"],
      "properties": {
        "kind": {
          "type": "string",
          "enum": ["hex", "base64", "fernet"]
        },
        "length": {
          "type": "integer",
          "minimum": 1,
          "description": "Bytes; default 32."
        }
      }
    },
    "paramEnumOption": {
      "type": "object",
      "additionalProperties": false,
      "required": ["value"],
      "properties": {
        "value": { "type": "string" },
        "label": { "type": "string" },
        "description": { "type": "string" }
      }
    },
    "appEnvVar": {
      "type": "object",
      "additionalProperties": false,
      "required": ["key", "value", "isSecret"],
      "properties": {
        "key": { "type": "string", "minLength": 1 },
        "value": { "type": "string" },
        "isSecret": { "type": "boolean" },
        "description": { "type": "string" },
        "label": { "type": "string" },
        "type": { "$ref": "#/$defs/paramType" },
        "required": {
          "type": "boolean",
          "description": "Tri-state: true/false/absent. Absent means legacy \"isSecret implies required\"."
        },
        "advanced": { "type": "boolean" },
        "placeholder": { "type": "string" },
        "pattern": {
          "type": "string",
          "description": "string type: regex the value must match."
        },
        "minLength": { "type": "integer", "minimum": 0 },
        "maxLength": { "type": "integer", "minimum": 0 },
        "min": {
          "type": "integer",
          "description": "integer/port type."
        },
        "max": {
          "type": "integer",
          "description": "integer/port type."
        },
        "options": {
          "type": "array",
          "description": "enum type.",
          "items": { "$ref": "#/$defs/paramEnumOption" }
        },
        "trueValue": { "type": "string", "description": "boolean type." },
        "falseValue": { "type": "string", "description": "boolean type." },
        "httpsOnly": { "type": "boolean", "description": "url type." },
        "generate": {
          "$ref": "#/$defs/paramGenerate",
          "description": "Only meaningful when isSecret is true."
        }
      }
    }
  }
}
